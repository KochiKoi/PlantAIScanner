version: '3.8'

services:
  backend:
    build: ./Backend
    ports:
      - "3000:3000"
    volumes:
      - ./Backend:/app
      - /app/node_modules # Giữ thư viện bên trong container, tránh xung đột với máy ngoài
    environment:
      - NODE_ENV=development

